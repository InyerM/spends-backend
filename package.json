{
  "name": "expense-assistant",
  "version": "1.0.0",
  "description": "Telegram bot for tracking expenses with Gemini and Google Sheets",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "import": "node scripts/import-csv.js"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "google-auth-library": "^9.6.3",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
